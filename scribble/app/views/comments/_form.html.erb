<%= form_for ([@user, @comments]) do |f| %>
  <%= f.label :subject %>
  <%= f.text_field :subject %>

  <%= f.label :comment %>
  <%= f.text_field :comment %>

  <%= f.hidden_field :user_id, :value => @user.id %>

  <%= f.submit "Create" %>
<% end %>
